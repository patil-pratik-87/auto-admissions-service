[project]
name = "admissions-full-agentic-prototype"
version = "0.1.0"
description = "Clean-room two-agent LLM prototype for admissions screening, evaluated side by side against the rule-based system in app/"
requires-python = ">=3.12,<3.13"
dependencies = [
    "openai>=2",
    "pydantic>=2",
    "langgraph>=0.4",
    "langsmith>=0.3",
    "python-dotenv>=1",
    "jupyter>=1",
    "nbformat>=5.10",
    "pandas>=3.0.5",
]
